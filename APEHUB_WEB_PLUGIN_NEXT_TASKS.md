# ApeHub Web 插件下一阶段整改任务

## 1. 评估结论

当前插件已经具备官网静态入口、后台管理页面、文档管理、插件审核、订单、提现和用户查询等基础骨架；但核心业务仍处于“演示可见、生产未闭环”状态。最需要优先处理的是：

1. 官网静态页大量使用硬编码数据和演示弹窗，未接入已有 API。
2. 登录/注册链路不完整，当前页面显示“演示成功”，不能真正登录。
3. 支付回调、分账和提现存在状态一致性与资金安全风险。
4. 插件上传缺少大小、类型、内容和版本校验，无法形成可发布包治理。
5. 菜单、路由、组件标识与插件版本升级约束不够明确，容易再次出现 404 或重复安装异常。

## 2. P0：必须先整改

### 2.1 官网页面接入真实数据

**现状证据**

- `static/plugins.html` 在脚本中维护固定的 `PLUGINS` 数组和虚构下载量/评分，未调用 `/site/public/plugins`。
- `static/profile.html` 固定显示 `user@apeadmin.com`、余额、收入和订单记录。
- `static/plugin-detail.html` 使用固定 `PLUGINS` 对象，找不到名称时回退到 `AI Chat Engine`。

**开发任务**

- 插件市场改为调用 `/api/v1/apehub-web/site/public/plugins`，支持服务端分页、关键词、分类筛选。
- 分类标签调用 `/site/public/plugins/categories`，不要在前端写死分类编码。
- 详情页使用 URL 中的插件 ID 调用 `/site/public/plugins/{id}`，移除按名称查找和默认回退数据。
- 个人中心调用 `/profile`、`/orders/my/paid`、`/orders/my`、`/incomes`、`/withdrawals`，未登录时跳转真实登录入口。
- 首页的站点名称、SEO、Hero、Features、Footer 使用 `/site/public/config` 和 `/site/public/content` 渲染。
- 所有接口增加 loading、空数据、错误、重试和登录失效处理。

**完成标准**

- 数据库新增、修改、下架插件后，官网刷新即可反映变化。
- 清空数据库演示数据后，页面显示真实空状态，不出现虚构插件、余额或用户。
- 详情页访问不存在的 ID 返回明确的 404 页面，不回退到其他插件。

### 2.2 完成真实登录、注册和验证码闭环

**现状证据**

- `static/index.html` 的登录提交仅弹出“登录成功（演示）”。
- 注册仅调用前端演示逻辑；发送验证码也只是倒计时和 alert。
- 后端仅提供注册接口，没有与官网登录表单对应的登录接口调用方案。

**开发任务**

- 登录表单调用 ApeAdmin 正式登录接口，保存 JWT，并统一使用现有认证存储方式。
- 注册表单调用 `/site/auth/register`，成功后使用返回 token 或跳转登录，不能直接伪造成功提示。
- 若保留邮箱验证码，新增发送验证码接口、验证码校验、频率限制、IP/邮箱风控和失败提示；验证码必须服务端校验，不能仅靠前端倒计时。
- 表单明确区分登录和注册字段，补齐必填校验、密码强度、重复提交锁定和 401/422 错误提示。
- 登录成功后跳转个人中心或用户指定的原始页面，退出登录清理 token。

**完成标准**

- 使用错误账号/密码一定失败；刷新页面后登录态可恢复；退出后受保护接口不可访问。
- 注册用户名、邮箱重复时返回可理解的错误；验证码过期、错误、重复使用均失败。

### 2.3 修复支付回调和资金状态机

**现状证据**

- `/notify` 验签后未校验回调金额、商品、商户号与订单金额是否一致。
- 回调并发时存在重复创建收入、重复增加余额的风险。
- 提现处理只允许 `pending` 状态；一旦 `approve` 后状态变为 `approved`，后续 `done` 无法执行。
- 提现拒绝/完成缺少并发锁和幂等约束。

**开发任务**

- 回调严格校验订单号、商户号、金额、币种、交易状态和支付渠道交易号。
- 为订单号、渠道交易号、收入记录增加唯一约束；使用事务和行锁保证回调幂等。
- 明确订单状态机：`pending -> paid/cancelled/refunded`，拒绝重复支付和非法迁移。
- 明确提现状态机：`pending -> approved -> done`，`pending -> rejected`；只有合法前置状态可操作。
- 提现操作增加审计人、审计时间、备注；失败或拒绝时准确释放冻结余额。
- 金额统一使用 Decimal 或定点数，禁止使用 Float 做结算核心计算。
- 支付密钥、SMTP 授权码只写入/更新，不在查询接口回传明文；配置保存时空值不能覆盖已有密钥。

**完成标准**

- 同一支付通知重复发送不会重复入账。
- 金额被篡改、签名错误、订单不存在、交易号重复时均拒绝并记录日志。
- 提现可以完整走完“审核通过→已打款”，余额、冻结金额、累计提现金额始终一致。

## 3. P1：发布能力与安全整改

### 3.1 插件包、文件和版本治理

- 上传限制总大小、单文件大小、扩展名、MIME、压缩包路径穿越和压缩炸弹。
- ZIP 上传后校验 `plugin.json`、入口模块、版本号、插件名称、菜单路径和依赖，失败时删除临时文件。
- `file_type=package` 每个插件/版本只能有一个有效包；文档、截图与包建立明确关联。
- 插件版本采用唯一键：`plugin_id + version`，同版本重新导入必须明确“拒绝 / 覆盖 / 生成修订版”，不能静默产生重复记录。
- 发布新版本时保留旧版本下载记录和订单授权；下架不能让已购买用户失去合法下载权。
- 下载接口使用安全路径解析，禁止读取上传目录之外的文件；增加下载计数的幂等策略。
- 上传、删除、审核、上下架全部记录操作人和审计日志。

### 3.2 权限、菜单和路由兼容

- 菜单标题仅用于展示，新增全局唯一的 `menu_key`、`route_name` 和 `permission`。
- 插件菜单可以与系统菜单同名，但路由标识必须带插件前缀，例如 `apehub_web_users`。
- 所有菜单组件路径统一使用可解析的相对路径，导入 ZIP 后执行组件存在性检查。
- 禁用插件后必须同时隐藏菜单、注销 API/静态路由、清理前端动态路由缓存；已打开页面应显示插件已停用，而不是 500/白屏。
- 启用、禁用、卸载、重装和同版本升级都要验证菜单、权限、数据库迁移的幂等性。

### 3.3 后台管理页面补齐业务能力

- 配置页对敏感字段使用“已配置/未配置”状态，不把空密码写回后端。
- 内容管理支持按区块筛选、排序、启停、预览和草稿发布，避免保存半成品直接影响官网。
- 文档管理支持 Markdown 编辑预览、XSS 过滤、分类删除保护和发布状态切换。
- 插件审核页展示包文件、版本、变更说明、权限/菜单清单和审核历史。
- 用户、订单、提现、收入列表统一支持筛选、分页、导出或审计详情；禁止 N+1 查询。
- 修正 API 与页面字段命名，统一 `display_name/name/slug` 的含义。

## 4. P2：体验与运维

- 静态官网改为可配置站点前缀，禁止在 HTML 中散落 `/apehub-web` 硬编码。
- 所有页面统一中文文案、错误码和空状态；移除“演示”“虚构数据”“100% AI Generated”等上线文案。
- 增加健康检查、结构化日志、请求 ID、支付回调日志和插件生命周期日志。
- 增加数据库迁移回滚/备份说明；迁移失败必须不破坏旧表和已有数据。
- 对公共接口增加限流、缓存和查询上限；对搜索关键词做长度限制和必要转义。
- 提供 OpenAPI/接口字段说明和前端联调示例，避免插件包内 API、宿主 API 两份定义漂移。

## 5. 必须补充的自动化测试

1. 官网公共接口：配置、内容、文档、分类、插件列表、详情、空状态、404。
2. 认证：登录成功/失败、注册重复、验证码错误/过期/重放、退出和 token 失效。
3. 插件生命周期：安装、启用、禁用、重启、卸载、重装、同版本更新、新版本升级。
4. 路由兼容：插件菜单与系统菜单同名时，系统页面和插件页面均可访问且刷新不 404。
5. 文件安全：超限、非法扩展名、恶意 ZIP、路径穿越、未购买下载、文件删除回收。
6. 支付：签名错误、金额不一致、重复通知、并发通知、取消、退款和收入入账。
7. 提现：余额不足、重复提交、审核通过、拒绝退回、完成打款、非法状态迁移。
8. 前端构建和浏览器回归：桌面/移动端、刷新、深链直达、401/403/404/500 页面。

## 6. 交付要求

插件方每次提交必须提供：

- 版本号、数据库迁移号和升级说明。
- API 变更清单及前后端字段示例。
- 菜单 `menu_key`、路由 `route_name`、组件路径、权限清单。
- 测试报告和关键页面截图/录屏。
- 从旧版本升级、回滚、禁用、卸载、同版本重导入的验证结果。

建议交付顺序：先完成 P0 真实业务闭环，再完成 P1 安全与发布治理，最后处理 P2 体验和运维。未完成 P0 前，不应把官网标记为可正式上线。
